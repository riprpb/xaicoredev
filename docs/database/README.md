# Database Documentation

PostgreSQL and Prisma are the active transactional data direction. Gate 1 will document
schema ownership, migration workflow, Decimal serialization, backups, retention,
encryption, audit storage, and recovery testing before production data is stored.
