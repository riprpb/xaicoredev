# API Documentation

Gate 0 exposes only public identity and health endpoints. Gate 1 will define versioned
API conventions, authentication, authorization, error envelopes, idempotency,
pagination, rate limits, and OpenAPI generation before account endpoints are added.
